#!/usr/bin/env ruby

File.write(
  File.join(".", "index.js"),
  Dir[File.join(".", "*Type.js")].map do |path|
    path.gsub(".js", "").gsub("./", "")
  end.map do |name|
    "export {#{name}} from \"./#{name}\""
  end.join("\n")
)
